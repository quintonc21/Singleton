/*#include <iostream>
#include <cctype>
#include <string>

void removePunctuation(std::string & sentence)
{
    for (int i = 0; i <= sentence.length(); i++)
    {
        if (ispunct(sentence[i]))
        {
            sentence.erase(i, 1);
            i--;
        }
    }
}

int main()
{
    std::string sentence = "Hello, how are you??";
    removePunctuation(sentence);
    if (sentence == "Hello how are you")
        return 0;
    else
    {
        std::cout << "When removePunctuation was called using the sentence \"Hello, how are you?\", it returned, \"" << sentence<< "\"";
        return -1;
    }
}
*/

/*include io stream 
include matrix stuff
just run the thing a few times with some examples, 
autograde and see if they want something else here*/

#include <iostream>


int main() 
{

}